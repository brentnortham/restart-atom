# restart-atom package

A simple package to restart the atom application and reopen windows as they were when exiting.

Install:
```bash
apm install restart-atom
```

Press `CTRL-ALT-X` in the workspace to restart Atom.
